[package]
name = "confit-apply"
version = "0.8.0"
edition = "2024"

[lints]
workspace = true

[dependencies]
confit-core = { version = "0.8.0", path = "../core" }
confit-store = { version = "0.8.0", path = "../store" }
dirs = "7.0.0"
serde_json.workspace = true

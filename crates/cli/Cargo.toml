[package]
name = "confit-cli"
version = "0.4.0"
edition = "2024"

[lints]
workspace = true

[dependencies]
anstream.workspace = true
clap = { workspace = true, features = ["derive"] }
confit-core = { path = "../core" }
confit-engine = { path = "../engine" }
log.workspace = true
serde_json.workspace = true

[dev-dependencies]
tempfile.workspace = true
